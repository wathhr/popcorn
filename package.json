{
  "license": "GPL-3.0-or-later",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "devserver": "node scripts/devserver.mjs",
    "bundle": "node scripts/bundle.mjs",
    "build:main": "cross-env NODE_ENV=production node scripts/bundle.mjs --types=main --minify",
    "build:preload": "cross-env NODE_ENV=production node scripts/bundle.mjs --types=preload --minify",
    "build:renderer": "cross-env NODE_ENV=production node scripts/bundle.mjs --types=renderer --minify",
    "build:all": "cross-env NODE_ENV=production node scripts/bundle.mjs --types=all --minify",
    "watch:main": "cross-env NODE_ENV=development node scripts/bundle.mjs --types=main --watch",
    "watch:preload": "cross-env NODE_ENV=development node scripts/bundle.mjs --types=preload --watch",
    "watch:renderer": "cross-env NODE_ENV=development node scripts/bundle.mjs --types=renderer --watch",
    "watch:all": "cross-env NODE_ENV=development node scripts/bundle.mjs --types=all --watch"
  },
  "devDependencies": {
    "@types/node": "^18.16.19",
    "@types/ws": "^8.5.5",
    "@typescript-eslint/eslint-plugin": "^5.60.1",
    "@typescript-eslint/parser": "^5.60.1",
    "cross-env": "^7.0.3",
    "esbuild": "^0.17.19",
    "esbuild-svelte": "^0.7.4",
    "eslint": "^8.44.0",
    "monaco-types": "^0.1.0",
    "typescript": "^5.1.6",
    "user-agent-data-types": "^0.3.1",
    "ws": "^8.13.0"
  },
  "dependencies": {
    "@monaco-editor/loader": "^1.3.3",
    "@walter-org/svelte-float": "^0.0.3",
    "auto-bind": "^5.0.1",
    "chokidar": "^3.5.3",
    "fast-glob": "^3.3.0",
    "svelte": "^3.59.2",
    "svelte-icons-pack": "^2.1.0",
    "svelte-preprocess": "^5.0.4",
    "ts-debounce": "^4.0.0"
  },
  "optionalDependencies": {
    "w3c-css-validator": "^1.3.1"
  }
}
